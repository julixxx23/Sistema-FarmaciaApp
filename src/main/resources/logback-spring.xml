<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http class="com.github.loki4j.logback.BasicAuthHttpSender">
            <url>${LOKI_URL:-https://logs-prod-042.grafana.net/loki/api/v1/push}</url>
            <username>${LOKI_USERNAME:-1499882}</username>
            <password>${LOKI_PASSWORD:-}</password>
        </http>
        <format>
            <label>
                <pattern>app=AppOchoa,env=production,level=%level</pattern>
            </label>
            <message>
                <pattern>%d{HH:mm:ss} %-5level %logger - %msg%n</pattern>
            </message>
        </format>
    </appender>
</configuration>